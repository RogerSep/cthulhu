import CollaborativeItem from './CollaborativeItem';
import { expect } from 'chai';

describe('CollaborativeItem component', () => {
  it('should be tested right now!', () => {
    expect(true).to.equal(true);
  });
});
